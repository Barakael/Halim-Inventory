module.exports = {
  apps: [
    {
      name: "haslim-inventory",
      cwd: "/var/www/haslim-inventory",
      script: "server.js",
      instances: 1,
      exec_mode: "fork",
      autorestart: true,
      watch: false,
      max_memory_restart: "300M",
      restart_delay: 5000,
      env: {
        NODE_ENV: "production",
        PORT: 40000,
      },
    },
  ],
};
